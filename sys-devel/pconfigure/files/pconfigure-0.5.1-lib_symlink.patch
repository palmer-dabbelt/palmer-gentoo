commit 603369b5818e07dcce3b81691c619c175c3c64af
Author: Palmer Dabbelt <palmer@dabbelt.com>
Date:   Sun Nov 17 21:16:28 2013 -0800

    Follow symlinks when resolving scala libraries
    
    This follows symlinks that come directly on the commandline, but not
    otherwise.  This handles the case where we have /usr/lib as a symlink,
    but avoids directory loops (which are apparently common in /usr/lib).

diff --git a/src/pscala/compiler.bash b/src/pscala/compiler.bash
index 2b697c8..a848765 100644
--- a/src/pscala/compiler.bash
+++ b/src/pscala/compiler.bash
@@ -12,7 +12,7 @@ do
 	shift
     elif [[ "$1" == "-l" ]]
     then
-	jar="$(find $jarpath -name "$2".jar; find $jarpath -name lib"$2".jar | head -n1)"
+	jar="$(find -H $jarpath -name lib"$2".jar | head -n1)"
 	jars="$jar:$jars"
 	shift
 	shift
commit 2aa409056840bd215cf50647d52c4d216d3b7b00
Author: Palmer Dabbelt <palmer@dabbelt.com>
Date:   Sun Nov 17 21:29:46 2013 -0800

    Apply the same library fix to pscalald

diff --git a/src/pscala/linker.bash b/src/pscala/linker.bash
index 527d67e..9db5990 100644
--- a/src/pscala/linker.bash
+++ b/src/pscala/linker.bash
@@ -24,7 +24,7 @@ do
 	shift
     elif [[ "$1" == "-l" ]]
     then
-	jar="$(find $jarpath -name "$2".jar; find $jarpath -name lib"$2".jar)"
+	jar="$(find -H $jarpath -name lib"$2".jar)"
 	jars="$jar:$jars"
 	shift
 	shift
